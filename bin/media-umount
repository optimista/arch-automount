#!/bin/sh
#$1 = <dev>
#$2 = Label

# Directory to use for umounting the devices
dev="/dev/$1"
label=$( echo $2 | tr '[:upper:]' '[:lower:]' )
dir="/media/$label"

umount "$dev"
rmdir "$dir"

for u in /home/*; do
	rm "$u/@$label"
done

exit 0
